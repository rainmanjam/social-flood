# Social Flood API - Development Dependencies
# Install with: pip install -r requirements-dev.txt
# Last updated: 2026-09-01

# Include production dependencies
-r requirements.txt

# Testing
pytest==9.1.1
pytest-cov==7.1.0
pytest-asyncio==1.4.0

# Code Quality
flake8==7.3.0
black==26.5.1
isort==9.0.1

# Type Checking
mypy==2.3.1

# Security
pip-audit==2.10.1

# Development Tools
# httpie was removed: 3.2.4 is the latest release and still carries
# PYSEC-2023-242 with no fix available. Use curl/httpx instead.
watchfiles==1.2.0

# PostgreSQL driver, DEV ONLY. The async DB layer (app/core/database.py) was
# deleted; the sole remaining reference is an optional `try: import psycopg2`
# in scripts/check_env.py's connectivity probe. It was a production pin, so a
# ~10 MB binary wheel shipped in every image for a dev script that degrades
# gracefully without it.
psycopg2-binary==2.9.12
