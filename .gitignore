# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environment
venv/
.env
.venv/
ENV/
env.bak/
env/

# VS Code settings
.vscode/

# Pytest cache and testing results
.cache/
.pytest_cache/

# MyPy cache
.mypy_cache/

# Caches
*.log
*.tmp

# IDE specific
.idea/
*.swp

# Jupyter Notebook
.ipynb_checkpoints

# Docker files
*.env
docker-compose.override.yml

# FastAPI / Uvicorn logs
*.log
logs/

# Database
*.db
*.sqlite3

# Redis dump files
dump.rdb

# Configurations and Keys
# config/config.py removed - use app/core/config.py instead

# Environment variable files
# NOTE: `*.env` matches `.env` itself. Do NOT re-include with a broad `!.env.*`
# negation -- that un-ignores `.env.bak`, `.env.local`, `.env.production`, etc.
# Only the placeholder template is tracked.
*.env
.env
.env.*
!.env.example

# Coverage
.coverage
coverage/
htmlcov/

# Jupyter Notebook
.ipynb_checkpoints/

# macOS files
.DS_Store

# Testing outputs
test_output/

# Cosign keys
cosign.key
cosign.pub

# Documentation
# docs/ is tracked: README links into docs/features/ and docs/comparisons/,
# which 404 for anyone cloning while the whole tree is ignored.
# These two subtrees stay out -- they are scraped third-party business data
# and enrichment-tool research notes, not project documentation.
docs/reports/
docs/research/

# Committed NLTK corpora are not needed: the Dockerfile downloads its own copy
# into /opt/nltk_data, and google_news_api.py downloads on demand when absent.
nltk_data/

# Scratch output
test_output.txt

# Agent / code-intelligence tool output (session-local, not project state)
.claude/
.serena/
.gitnexus/
graphify-out/
AGENTS.md
